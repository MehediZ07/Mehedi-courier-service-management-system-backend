model Visit {
  id        String   @id @default(uuid())
  ip        String?
  userAgent String?
  userId    String?
  userEmail String?
  userName  String?
  userRole  String?
  page      String?
  createdAt DateTime @default(now())

  @@index([createdAt])
  @@index([userId])
}
