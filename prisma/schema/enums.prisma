enum Role {
  SUPER_ADMIN
  ADMIN
  COURIER
  MERCHANT
  USER
}

enum UserStatus {
  ACTIVE
  INACTIVE
  SUSPENDED
}

enum ApprovalStatus {
  PENDING
  APPROVED
  REJECTED
}

enum ShipmentStatus {
  PENDING
  ASSIGNED
  PICKED_UP
  IN_TRANSIT
  OUT_FOR_DELIVERY
  DELIVERED
  FAILED
  RETURNED
}

enum Priority {
  STANDARD
  EXPRESS
}

enum PaymentStatus {
  PENDING
  PAID
  COD
  FAILED
}

enum PaymentMethod {
  STRIPE
  COD
}

enum VehicleType {
  BIKE
  BICYCLE
  CAR
  VAN
  TRUCK
}

enum RegionType {
  LOCAL
  NATIONAL
  INTERNATIONAL
}

enum HubType {
  LOCAL         // City-level hub
  REGIONAL      // Province/state hub
  INTERNATIONAL // Country-level hub
}

enum LegType {
  DIRECT        // Sender → Recipient (same city)
  PICKUP        // Sender → Hub
  HUB_TRANSFER  // Hub → Hub
  DELIVERY      // Hub → Recipient
}

enum LegStatus {
  PENDING
  ASSIGNED
  IN_PROGRESS
  COMPLETED
  FAILED
}

enum LocationType {
  ADDRESS
  HUB
}

enum DeliveryType {
  LEGACY_DIRECT // Old system (backward compatible)
  DIRECT        // New single-leg delivery
  HUB_BASED     // Multi-leg delivery
}
